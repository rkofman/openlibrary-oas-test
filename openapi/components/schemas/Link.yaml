type: object
required:
  - url
  - title
additionalProperties: false
properties:
  url:
    type: string
  title:
    type: string
  type:
    type: object,
    additionalProperties: false,
    required:
      - key
    properties:
      key:
        enum:
          - /type/link
